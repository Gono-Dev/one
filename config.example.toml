[server]
bind = "127.0.0.1:3000"
cert_file = "certs/cert.pem"
key_file = "certs/key.pem"
base_url = "https://127.0.0.1:3000"

[storage]
data_dir = "data"
xattr_ns = "user.nc"

[db]
path = "data/nc-dav.db"
max_connections = 5

[auth]
realm = "Nextcloud"
